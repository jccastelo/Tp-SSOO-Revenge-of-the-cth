#include "../include/configuraciones.h"

void leer_configuraciones(t_config_kernel *config_kernel)
{
    t_config* iniciar_config(void);
    void leer_configuraciones(t_config_kernel *config_kernel);
}